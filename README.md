# MY DOTFILES

This repo contains the current dotfiles I use in my linux setup.

Currently, I'm using a Debian setup with I3wm + i3blocks.

If you want to use the same setup as I do, run:

```bash
chmod +x setup.sh
./setup.sh
```

